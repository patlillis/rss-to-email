{
  "name": "example-worker",
  "version": "1.0.0",
  "description": "Example Cloudflare Worker with cron triggers",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run",
    "check": "tsc"
  },
  "dependencies": {
    "@aws-sdk/client-ses": "^3.525.0",
    "rss-parser": "^3.13.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240208.0",
    "typescript": "^5.8.3",
    "wrangler": "^4.10.0"
  }
}
